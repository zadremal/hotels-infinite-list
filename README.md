## Hotels List with infinite scroll loading and region filter

- generates 100 fake hotels
- loads hotels data per 10 items. Once scrolled to the end of the page - loads
  10 more
- allows to filter hotels by region

To run the app locally in development mode install npm dependencies 
first with ```npm install``` command, and execute ```npm run start``` 